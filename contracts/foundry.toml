[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.24"
# Ethereum Sepolia supports Cancun, which matches the WDK + USDT direction.
evm_version = "cancun"

[profile.default.fmt]
line_length = 100
